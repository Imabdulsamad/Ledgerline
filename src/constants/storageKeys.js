/** Namespaced localStorage keys — avoids collisions with other apps on the same origin. */
export const STORAGE_KEYS = {
  transactions: "ledgerline:transactions:v1",
  theme: "ledgerline:theme:v1",
  paidBills: "ledgerline:paid-bills:v1",
};
